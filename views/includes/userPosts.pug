each post in posts
  div(class='well')
    h4= post.posted.toLocaleDateString("en-US")
    h3= post.title
    p= post.content
  form(action='/posts/delete' method='post')
    input(type='hidden' class='form-control' name='postId' value='#{post._id}')
    button(type='submit' class='btn btn-warning btn-lg') Delete
